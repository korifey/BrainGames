<Window x:Class="PasswordToComport.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:melodyGuess="clr-namespace:PasswordToComport"
        Title="MainWindow" Width="640" Height="480"
        WindowStyle="None" ResizeMode="NoResize" WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Background="WHite" KeyUp="MainWindow_OnKeyUp">

  <Border BorderBrush="White" BorderThickness="0">
    <StackPanel Orientation="Horizontal">
      <TextBlock FontSize="100" Text="Password:" VerticalAlignment="Center"/>
      <TextBox FontSize="100" Name="Password" KeyDown="Password_OnKeyDown" HorizontalAlignment="Stretch" VerticalAlignment="Center" 
               MinWidth="600"
               Margin="100 0 0 0"/>
    </StackPanel>
  </Border>
</Window>